# AgentHarbor quality requirements

Follow the repository [quality policy](../../docs/quality.md). Commands there explicitly identify the backend and frontend working directories. Run regression tests for every behavior fix, and report missing AI configuration separately from application failures.
